"""inference-router reference slice.

The differentiated layer from the decision record: cost-aware cascade routing
(ADR-0002), multi-provider failover (ADR-0003/0004), a budget policy gate
(ADR-0005), and an exact-match cache (ADR-0006). Pure standard library — no API
keys, no network — so it runs anywhere.
"""
__version__ = "0.1.0"
